---
name: apple-design
description: Design or review Apple-platform and Apple-inspired interfaces using the bundled Human Interface Guidelines reference. Use for iOS, iPadOS, macOS, watchOS, tvOS, visionOS, SF Symbols, Liquid Glass, Dynamic Type, semantic colors, or Apple-like web UI.
---

# Apple Design

Read `../../SKILL.md` first. It is the shared HIG reference and routing table for this suite.

Use the smallest reference it names for the requested platform, component, pattern, or web work.
For a runnable workflow, use the sibling skills:

- `$apple-design-web` for HTML/CSS implementation.
- `$apple-design-review` for an audit with findings.
- `$apple-design-qa` for rendered visual checks.
- `$apple-design-refresh` to update Apple-derived references.
